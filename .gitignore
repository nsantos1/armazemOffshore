node_modules
.next
out
*.tsbuildinfo
.vercel
# Variáveis de ambiente com segredos (senha de e-mail, token do Turso, etc.)
.env
.env*.local
# Dados gravados em runtime (leads, conteúdo do CMS) — não versionar
/data
# Uploads de imagens feitos pelo admin em runtime
/public/uploads
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.claude